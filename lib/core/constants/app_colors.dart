import 'package:flutter/material.dart';

/// App color palette - Dark Neon Minimalism
class AppColors {
  // Private constructor to prevent instantiation
  AppColors._();

  // Background
  static const Color background = Color(0xFF0A0A0A); // Deep black

  // Primary Neon
  static const Color primaryNeon = Color(0xFF00FFB0); // Electric mint

  // Accent Neon
  static const Color accentNeon = Color(0xFF00BFFF); // Cyan glow

  // Secondary Text
  static const Color secondaryText = Color(0xFFA0A0A0); // Muted gray

  // Timer Active
  static const Color timerActive = Color(0xFFFF4B4B); // Alert red

  // Additional utility colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);

  // Button colors
  static const Color buttonBackground = Color(0xFF1A1A1A);
  static const Color buttonDisabled = Color(0xFF333333);
}
